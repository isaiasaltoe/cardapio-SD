apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-api
  namespace: cardapio
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - cardapio
    labelSelectors:
      app: api
