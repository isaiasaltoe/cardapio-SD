apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: stress-api
  namespace: cardapio
spec:
  mode: all
  selector:
    namespaces:
      - cardapio
    labelSelectors:
      app: api
  stressors:
    cpu:
      workers: 2
      load: 80
    memory:
      workers: 1
      size: "256MB"
  duration: "60s"
